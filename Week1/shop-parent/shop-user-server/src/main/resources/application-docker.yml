# docker profile：容器内用服务名互访
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/shop-user?serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf-8
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
